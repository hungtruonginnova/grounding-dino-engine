# Host install (macOS/Apple Silicon): the PyPI torch wheel is MPS-capable.
# This is also what the Docker image installs (CUDA wheels) - the image is
# deployed only on GPU hosts (DGX Spark and friends).
torch==2.9.1
transformers==5.16.1
pillow==11.1.0
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.5
pytest==8.3.4
pytest-asyncio==0.25.2
